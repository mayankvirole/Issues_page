{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "build-app": "git pull && npm run build-client && npm install && npm start",
    "build-client": "mkdir -p public && cd issues_page && npm run build-app && cd ..",
    "start": "pm2 start index.js -n Vuetomate && pm2 monit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  }
}
